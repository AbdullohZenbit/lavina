import { Stack, styled } from "@mui/material";

export const SinUpContainer = styled(Stack)(({ theme }) => ({
  height: "100vh",
  width: "100vw",
  alignItems: "center",
  justifyContent: "center",
}));
